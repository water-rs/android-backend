# Android backend local build artifacts
/.gradle/
/.kotlin/
/.idea/
/.cxx/
/build/
.cxx/
.gradle/
.idea/
*.iml
local.properties
*.keystore
.DS_Store
/runtime/build
.ci-logs/
.ci-artifacts/
# Staged by CI for the instrumentation tests; the app crate's .so is built
# from a WaterUI checkout and must never be committed.
/runtime/src/androidTest/jniLibs/
__pycache__/
