{
  "$schema-comment": "Compose MD3 parity budgets per twinned example. An example listed here must have a twin registered in e2e/reference (Twins.kt); the nightly renders the twin on the same emulator and pixel-compares it against the WaterUI capture. 'default' is the strict fallback fraction; raise an entry only alongside a filed issue for the divergence it blesses.",
  "default": 0.02,
  "examples": {
    "gesture": 0.02
  }
}
